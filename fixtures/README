This folder contains text fixtures.

The *yaml files are configuration files
The *key files are private keys, referred to in configuration
The *crt files are public keys, referred to in configuration

*-bad.yaml should not load for various reasons

*-good.yaml should all load successfully
